// Contact page is a client component, so its metadata lives here.
export const metadata = {
  title: "Contact",
  description:
    "Tell me about your engineering or automation problem. Direct contact with Dave Bradbury, Darbury Ltd — no sales team, no gatekeeping.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
